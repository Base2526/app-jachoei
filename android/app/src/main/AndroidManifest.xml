<manifest
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Camera (take photo in chat) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Voice messages (microphone recording) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!--
      Google Play (SMS/Call Log) compliance intent (Jachoei):
      - Not a default SMS app, not a default phone app, not an assistant app.
      - Minimal permissions: RECEIVE_SMS only (no READ_SMS / SEND_SMS / WRITE_SMS).
      - SMS is processed on-device only for anti-smishing scam detection and user warnings.
      - Does NOT intercept, abort, or prevent SMS delivery.
      - SMS content is not stored on Jachoei servers and is not shared with third parties.
      - User consent is required; permission can be revoked anytime.
    -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />

    <!-- Android 13+ notification permission for FCM notifications -->
    <uses-permission
        android:name="android.permission.POST_NOTIFICATIONS"
        tools:targetApi="33" />

    <!-- Enforce Play-compliant permission set even if a dependency adds them. -->
    <uses-permission android:name="android.permission.READ_CALL_LOG" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_SMS" tools:node="remove" />
    <uses-permission android:name="android.permission.SEND_SMS" tools:node="remove" />
    <uses-permission android:name="android.permission.WRITE_SMS" tools:node="remove" />
    <uses-permission android:name="android.permission.BROADCAST_SMS" tools:node="remove" />

    <application
        android:name=".MainApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true"
            android:theme="@style/BootTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep link: jachoei://chat/:conversationId -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="jachoei"
                    android:host="chat" />
            </intent-filter>
        </activity>

        <service
            android:name=".CallBlockerService"
            android:exported="true"
            android:permission="android.permission.BIND_SCREENING_SERVICE">
            <!-- Scam call screening/blocking via Android Telecom CallScreeningService. -->
            <intent-filter>
                <action android:name="android.telecom.CallScreeningService" />
            </intent-filter>
        </service>

        <receiver
            android:name=".SmsBlockerReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <!--
              Anti-smishing receiver:
              - Exported is required to receive the system SMS broadcast.
              - android:permission restricts senders to the system.
              - No high priority; does not abort or intercept delivery.
              - Use only what is necessary for scam detection.
            -->
            <intent-filter>
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
