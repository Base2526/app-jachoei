<resources>
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <!-- Prevent flashing the boot window background after startup -->
        <item name="android:windowBackground">@color/bootsplash_background</item>
        <item name="android:navigationBarColor">#0B1220</item>
        <!-- Prevent Android 10+ from forcing a light nav bar for contrast -->
        <item name="android:enforceNavigationBarContrast">false</item>
        <!-- Ensure nav bar icons are light on dark background -->
        <item name="android:windowLightNavigationBar">false</item>
    </style>

    <style name="BootTheme" parent="Theme.BootSplash">
        <item name="bootSplashBackground">@color/bootsplash_background</item>
        <item name="bootSplashLogo">@drawable/bootsplash_logo</item>
        <item name="postBootSplashTheme">@style/AppTheme</item>
    </style>
</resources>
